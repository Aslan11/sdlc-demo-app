# Task Tracker API

## Project Conventions

- **Runtime:** Node.js 18+
- **Framework:** Express 4.x
- **Tests:** Node.js built-in test runner (`node --test`)
- **Date handling:** Always use Date objects for date comparisons — never compare date strings directly
- **API style:** RESTful JSON, standard HTTP status codes
- **Commit messages:** Use imperative mood, reference issue numbers with `Fixes #N`
